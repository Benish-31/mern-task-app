{
    "name": "mern-task-app",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "scripts": {
        "start": "cd taskapp && npm start",
        "prebuild": "cd taskapp && npm install",
        "build": "cd taskapp/src/frontend && npm install && npm run build"
    }
}